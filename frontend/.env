# Use local for localhost/LAN field testing. Use remote for deployed API builds.
VITE_API_MODE=remote

# Set this to your backend origin when the API is hosted separately and VITE_API_MODE=remote.
VITE_API_URL=https://api.intuiticorporates.com
