# Leave empty to use the Vite dev proxy or a same-origin production reverse proxy.
# Set this to your backend origin when the API is hosted separately, for example:
# VITE_API_URL=http://localhost:8080
VITE_API_URL=http://13.207.57.23
